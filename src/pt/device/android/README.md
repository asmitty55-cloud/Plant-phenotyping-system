# Android Capture Client

This directory contains source files for the on-device capture activity. Build outputs
such as `.class`, `.apk`, and `.idsig` files are generated artifacts and should stay
outside git.
